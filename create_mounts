#!/bin/bash

set -euvx

HERE=$(realpath "${BASH_SOURCE%/*}")
SITES="${HERE}/sites/"
YARDS="${HERE}/yards/"
WAGONS="${HERE}/wagons/"

mkdir --parents "${SITES}/"{001..3} "${WAGONS}/"{800..803} "${YARDS}/"{900..901}
